server:
  port: 5005


spring:
  application:
    name: flight_ops_service

  datasource:
    url: jdbc:mysql://localhost:3306/airline_flight_db
    username: root
    password: Pradeep@drait123

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true