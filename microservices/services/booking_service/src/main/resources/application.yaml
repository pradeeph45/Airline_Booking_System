server:
  port: 5009

spring:
  application:
    name: booking_service

  datasource:
    url: jdbc:mysql://localhost:3306/airline_booking_db
    username: root
    password: root

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
