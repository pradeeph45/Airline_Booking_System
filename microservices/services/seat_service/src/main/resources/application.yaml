server:
  port: 5007

spring:
  application:
    name: seat_service

  datasource:
    url: jdbc:mysql://localhost:3306/airline_seat_db
    username: root
    password: root

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true