server:
  port: 5002

spring:
  application:
    name: user_service

  datasource:
    url: jdbc:mysql://localhost:3306/airline_db
    username: root
    password: root

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true