server:
  port: 5001

spring:
  application:
    name: user_service

  datasource:
    url: jdbc:mysql://localhost:3306/airline_location_db
    username: root
    password: root

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
