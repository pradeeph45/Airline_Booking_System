server:
  port: 5004

spring:
  application:
    name: location_service

  datasource:
    url: jdbc:mysql://localhost:3306/airline_location_db
    username: root
    password: root

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
